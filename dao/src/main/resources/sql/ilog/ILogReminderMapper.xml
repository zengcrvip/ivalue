<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.axon.market.dao.mapper.ilog.ILogReminderMapper">

    <insert id="insertLogReminder">
        insert into sms_notice.notice_sms_send(phone,module_id,sms_content,sms_content_detail)
             values (#{phone},#{moduleId},#{smsContent},#{smsContent})
    </insert>

</mapper>